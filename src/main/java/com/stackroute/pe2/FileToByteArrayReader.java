package com.stackroute.pe2;

/**
 * Practice Exercise - Question 8
 * Write a program to find all files of a folder and select only
 * given extension fileName and read content of this file using byte array
 */
public class FileToByteArrayReader {

    /**
     * Method to find file based on the file name and extension
     * given and returns the contents of the file as a byte array.
     * Throws FileNotFoundException if the file is not found.
     * @param filename Name of the file to be read
     * @param extension Extension of the file to be read.
     * @return Byte array containing the contents of the file
     */
    public byte[] readFile(String filename, String extension) {
        /*Should throw FileNotFoundException if the file is not found*/
        return null;
    }
}
